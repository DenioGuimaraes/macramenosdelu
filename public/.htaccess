RewriteEngine On

# ------------------------------------------------------------
# Front controller — usado quando o Document Root aponta
# diretamente para /public (configuração comum em hospedagem)
# ------------------------------------------------------------
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
